name: Build Remote Studio Agent

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: virtual-studio-agent
        run: npm install

      - name: Build for Windows
        working-directory: virtual-studio-agent
        run: npm run build:win

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: remote-studio-agent-win
          path: virtual-studio-agent/dist/*.exe

  build-mac:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: virtual-studio-agent
        run: npm install

      - name: Build for macOS
        working-directory: virtual-studio-agent
        run: npm run build:mac

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: remote-studio-agent-mac
          path: virtual-studio-agent/dist/*.dmg

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxtst-dev libpng-dev

      - name: Install dependencies
        working-directory: virtual-studio-agent
        run: npm install

      - name: Build for Linux
        working-directory: virtual-studio-agent
        run: npm run build:linux

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: remote-studio-agent-linux
          path: virtual-studio-agent/dist/*.AppImage

  release:
    needs: [build-windows, build-mac, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: remote-studio-agent-win
          path: ./dist

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: remote-studio-agent-mac
          path: ./dist

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: remote-studio-agent-linux
          path: ./dist

      - name: List all artifacts
        run: ls -la dist/

      - name: Rename files for consistent download URLs
        run: |
          cd dist
          for f in *.exe; do mv "$f" "Remote.Studio.Agent.Setup.exe" 2>/dev/null || true; done
          for f in *.dmg; do mv "$f" "Remote.Studio.Agent.dmg" 2>/dev/null || true; done
          for f in *.AppImage; do mv "$f" "Remote.Studio.Agent.AppImage" 2>/dev/null || true; done
          ls -la

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Remote Studio Agent ${{ github.ref_name }}
          body: |
            ## Remote Studio Agent ${{ github.ref_name }}
            
            Download the installer for your operating system:
            - **Windows**: Remote.Studio.Agent.Setup.exe
            - **macOS** (10.15+): Remote.Studio.Agent.dmg
            - **Linux**: Remote.Studio.Agent.AppImage
          files: |
            dist/Remote.Studio.Agent.Setup.exe
            dist/Remote.Studio.Agent.dmg
            dist/Remote.Studio.Agent.AppImage
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
